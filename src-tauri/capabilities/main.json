{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "main-capability",
    "description": "Capability for the main window",
    "windows": ["main"],
    "permissions": ["dialog:allow-open", "path:allow-resolve-directory", "event:allow-listen", "webview:allow-internal-toggle-devtools"]
}
